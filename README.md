Build a Movie Management Application backed by a RESTful API and a relational
database. The application should allow users to view, search, create, update, and
delete movies, using the supplied JSON dataset as a starting point. There
